## File Rules

- Do not read any file not explicitly mentioned — ask the user first.

## Command Rules

- Do not run any bash command — ask the user first.

## Implementation Rules

- Never write or modify files under `src/main` without first running the `/tdd-phase` skill.

## Design Rules

- Never reverse a decision recorded in `docs/orchestrator/discuss.md`.
- If an implementation would require adopting a rejected alternative from an ADR, stop and explain why to the user before proceeding.
